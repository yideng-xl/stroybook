# TODO: MVP1 后续改进计划

## 待办事项
- [ ] **自动化部署脚本**: 编写一个 `build-deploy.js`，在 build 完成后自动将 `../stories` 复制到 `dist/stories`，简化部署流程。
- [ ] **图片预加载**: 在阅读页增加图片预加载逻辑，避免翻页白屏。
- [ ] **音频支持**: (MVP4 需求预研) 调查 Web Speech API 或 TTS 服务集成。
- [ ] **状态持久化**: 记住用户的语言偏好（目前刷新后重置为默认）。

## 建议
- **图片压缩**: 目前直接加载原图，建议在扫描脚本中集成 sharp 库生成缩略图。
- **错误边界**: 增加 React Error Boundary，防止某个故事数据损坏导致全站崩溃。
